sum=0
kilos=float(input("How much do you weigh in kilos ? "))
while kilos!=0:
    sum+=kilos
    kilos=float(input("How much do you weight in kilos ? "))
print("The sum of the kid's weight is ",sum)
